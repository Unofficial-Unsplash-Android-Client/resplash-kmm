name: resplash kmm ci

on:
  push:
    branches:
      - master
      - develop
      - 'feature/**'
      - 'fix/**'

jobs:
  KTLint:
    runs-on: ubuntu-latest
    extends: ./ktlint.yaml

  buildDebugApk:
    needs: [KTLint]
    runs-on: ubuntu-latest
    extends: ./build_debug_apk.yaml
